# sre-agent-pause-mesh (Python agent peer)

Companion to `examples/java/sre-agent-pause-mesh`.

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['my-app']['v1.0.0']['dev']['base']"
python3 agent_peer.py
```

Leaf: `examples/sre-agent-pause-mesh/agent-tools` — change live; agent session stays up.
