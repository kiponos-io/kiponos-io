# mcp-tool-gate-live (Python agent peer)

Companion to `examples/java/mcp-tool-gate-live`.

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['my-app']['v1.0.0']['dev']['base']"
python3 agent_peer.py
```

Leaf: `examples/mcp-tool-gate-live/tools-allow` — change live; agent session stays up.
