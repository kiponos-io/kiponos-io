# client-server-mirror-live (Python agent peer)

Companion to `examples/java/client-server-mirror-live`.

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['my-app']['v1.0.0']['dev']['base']"
python3 agent_peer.py
```

Leaf: `examples/client-server-mirror-live/status` — change live; agent session stays up.
