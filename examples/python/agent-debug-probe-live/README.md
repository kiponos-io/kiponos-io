# agent-debug-probe-live (Python agent peer)

Companion to `examples/java/agent-debug-probe-live`.

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['my-app']['v1.0.0']['dev']['base']"
python3 agent_peer.py
```

Leaf: `examples/agent-debug-probe-live/verbose` — change live; agent session stays up.
