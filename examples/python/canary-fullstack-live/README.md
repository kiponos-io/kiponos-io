# canary-fullstack-live (Python agent peer)

Companion to `examples/java/canary-fullstack-live`.

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['my-app']['v1.0.0']['dev']['base']"
python3 agent_peer.py
```

Leaf: `examples/canary-fullstack-live/percent` — change live; agent session stays up.
