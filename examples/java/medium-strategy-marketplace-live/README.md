# medium-strategy-marketplace-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Marketplace War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-marketplace-live/active` (default `baseline`).
