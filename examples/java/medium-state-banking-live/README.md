# medium-state-banking-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Banking War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-banking-live/active` (default `baseline`).
