# medium-state-insurance-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Insurance War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-insurance-live/active` (default `baseline`).
