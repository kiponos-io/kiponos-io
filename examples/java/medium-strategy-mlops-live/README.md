# medium-strategy-mlops-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Mlops War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-mlops-live/active` (default `baseline`).
