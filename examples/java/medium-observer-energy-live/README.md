# medium-observer-energy-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Energy War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-energy-live/active` (default `baseline`).
