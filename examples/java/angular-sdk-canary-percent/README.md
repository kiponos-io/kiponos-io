# angular-sdk-canary-percent

Java peer for **traffic split percent** with the **@kiponos/angular** SDK as the write/UI side.

## Hub key

`release/canary-percent` (default `5`)

## Run

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['MyApp']['1.0']['Dev']['base']"
```

See `docs/examples/medium-drafts/angular-sdk-canary-percent.md` and `sdks/kiponos-angular-sdk`.
