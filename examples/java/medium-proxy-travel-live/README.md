# medium-proxy-travel-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-travel-live/active` (default `baseline`).
