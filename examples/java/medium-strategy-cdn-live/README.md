# medium-strategy-cdn-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Cdn War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-cdn-live/active` (default `baseline`).
