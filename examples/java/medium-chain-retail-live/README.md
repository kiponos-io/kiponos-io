# medium-chain-retail-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-retail-live/active` (default `baseline`).
