# medium-observer-retail-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-retail-live/active` (default `baseline`).
