# medium-decorator-saas-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Saas War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-saas-live/active` (default `baseline`).
