# medium-strategy-energy-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Energy War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-energy-live/active` (default `baseline`).
