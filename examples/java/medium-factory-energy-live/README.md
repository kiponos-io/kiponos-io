# medium-factory-energy-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Energy War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-energy-live/active` (default `baseline`).
