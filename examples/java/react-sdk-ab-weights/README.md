# react-sdk-ab-weights

Java peer for **experiment weight** with the **@kiponos/react** SDK as the write/UI side.

## Hub key

`checkout/ab-weight-b` (default `50`)

## Run

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['MyApp']['1.0']['Dev']['base']"
```

See `docs/examples/medium-drafts/react-sdk-ab-weights.md` and `sdks/kiponos-react-sdk`.
