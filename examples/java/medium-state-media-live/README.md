# medium-state-media-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-media-live/active` (default `baseline`).
