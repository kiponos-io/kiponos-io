# medium-observer-marketplace-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Marketplace War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-marketplace-live/active` (default `baseline`).
