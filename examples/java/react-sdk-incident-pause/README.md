# react-sdk-incident-pause

Java peer for **pause flag for agents** with the **@kiponos/react** SDK as the write/UI side.

## Hub key

`ops/incident/paused` (default `no`)

## Run

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['MyApp']['1.0']['Dev']['base']"
```

See `docs/examples/medium-drafts/react-sdk-incident-pause.md` and `sdks/kiponos-react-sdk`.
