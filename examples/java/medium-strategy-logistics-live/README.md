# medium-strategy-logistics-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-logistics-live/active` (default `baseline`).
