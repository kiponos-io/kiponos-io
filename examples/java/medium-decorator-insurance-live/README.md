# medium-decorator-insurance-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Insurance War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-insurance-live/active` (default `baseline`).
