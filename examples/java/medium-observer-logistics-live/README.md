# medium-observer-logistics-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-logistics-live/active` (default `baseline`).
