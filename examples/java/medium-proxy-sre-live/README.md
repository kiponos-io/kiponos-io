# medium-proxy-sre-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-sre-live/active` (default `baseline`).
