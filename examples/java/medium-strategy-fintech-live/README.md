# medium-strategy-fintech-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Fintech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-fintech-live/active` (default `baseline`).
