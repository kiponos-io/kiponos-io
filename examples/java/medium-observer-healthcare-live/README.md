# medium-observer-healthcare-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Healthcare War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-healthcare-live/active` (default `baseline`).
