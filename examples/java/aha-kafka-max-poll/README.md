# aha-kafka-max-poll

Kafka max poll records live

```bash
cd examples/java/aha-kafka-max-poll
cp kiponos.local.env.example kiponos.local.env
./gradlew test run
```

Hub key: `examples/aha-kafka-max-poll/max-poll-records` (default `50`).
