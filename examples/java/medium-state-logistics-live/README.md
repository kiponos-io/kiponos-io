# medium-state-logistics-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-logistics-live/active` (default `baseline`).
