# medium-observer-mlops-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Mlops War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-mlops-live/active` (default `baseline`).
