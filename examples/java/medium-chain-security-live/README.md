# medium-chain-security-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Security War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-security-live/active` (default `baseline`).
