# medium-observer-telecom-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Telecom War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-telecom-live/active` (default `baseline`).
