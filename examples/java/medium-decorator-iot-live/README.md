# medium-decorator-iot-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Iot War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-iot-live/active` (default `baseline`).
