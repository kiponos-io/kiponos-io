# medium-observer-iot-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Iot War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-iot-live/active` (default `baseline`).
