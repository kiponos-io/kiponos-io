# medium-factory-media-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-media-live/active` (default `baseline`).
