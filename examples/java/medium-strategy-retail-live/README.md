# medium-strategy-retail-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-retail-live/active` (default `baseline`).
