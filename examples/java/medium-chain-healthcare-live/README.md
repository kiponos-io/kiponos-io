# medium-chain-healthcare-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Healthcare War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-healthcare-live/active` (default `baseline`).
