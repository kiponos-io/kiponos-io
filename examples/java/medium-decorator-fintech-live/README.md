# medium-decorator-fintech-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Fintech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-fintech-live/active` (default `baseline`).
