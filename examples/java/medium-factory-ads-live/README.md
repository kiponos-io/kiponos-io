# medium-factory-ads-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Ads War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-ads-live/active` (default `baseline`).
