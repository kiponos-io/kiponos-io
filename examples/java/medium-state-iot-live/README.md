# medium-state-iot-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Iot War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-iot-live/active` (default `baseline`).
