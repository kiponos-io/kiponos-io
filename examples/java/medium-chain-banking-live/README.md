# medium-chain-banking-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Banking War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-banking-live/active` (default `baseline`).
