# medium-state-marketplace-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Marketplace War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-marketplace-live/active` (default `baseline`).
