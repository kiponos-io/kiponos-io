# aha-batch-chunk

Batch chunk size live

```bash
cd examples/java/aha-batch-chunk
cp kiponos.local.env.example kiponos.local.env
./gradlew test run
```

Hub key: `examples/aha-batch-chunk/chunk-size` (default `100`).
