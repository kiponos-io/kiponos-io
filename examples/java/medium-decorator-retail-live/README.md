# medium-decorator-retail-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-retail-live/active` (default `baseline`).
