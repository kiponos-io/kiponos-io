# medium-decorator-edtech-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Edtech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-edtech-live/active` (default `baseline`).
