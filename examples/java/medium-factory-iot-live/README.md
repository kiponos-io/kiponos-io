# medium-factory-iot-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Iot War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-iot-live/active` (default `baseline`).
