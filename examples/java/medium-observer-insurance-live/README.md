# medium-observer-insurance-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Insurance War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-insurance-live/active` (default `baseline`).
