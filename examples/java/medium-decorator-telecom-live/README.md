# medium-decorator-telecom-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Telecom War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-telecom-live/active` (default `baseline`).
