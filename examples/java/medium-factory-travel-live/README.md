# medium-factory-travel-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-travel-live/active` (default `baseline`).
