# medium-state-edtech-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Edtech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-edtech-live/active` (default `baseline`).
