# medium-factory-retail-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-retail-live/active` (default `baseline`).
