# medium-decorator-cdn-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Cdn War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-cdn-live/active` (default `baseline`).
