# medium-decorator-mlops-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Mlops War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-mlops-live/active` (default `baseline`).
