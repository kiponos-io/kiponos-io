# medium-chain-travel-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-travel-live/active` (default `baseline`).
