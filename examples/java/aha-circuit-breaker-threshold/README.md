# aha-circuit-breaker-threshold

Circuit breaker threshold live

```bash
cd examples/java/aha-circuit-breaker-threshold
cp kiponos.local.env.example kiponos.local.env
./gradlew test run
```

Hub key: `examples/aha-circuit-breaker-threshold/failure-threshold` (default `5`).
