# medium-factory-marketplace-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Marketplace War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-marketplace-live/active` (default `baseline`).
