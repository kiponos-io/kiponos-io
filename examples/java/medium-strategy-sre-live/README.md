# medium-strategy-sre-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-sre-live/active` (default `baseline`).
