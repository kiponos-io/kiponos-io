# medium-factory-edtech-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Edtech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-edtech-live/active` (default `baseline`).
