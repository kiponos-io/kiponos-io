# medium-state-security-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Security War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-security-live/active` (default `baseline`).
