# medium-chain-mlops-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Mlops War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-mlops-live/active` (default `baseline`).
