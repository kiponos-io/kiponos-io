# medium-decorator-logistics-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-logistics-live/active` (default `baseline`).
