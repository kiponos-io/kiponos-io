# medium-strategy-ads-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Ads War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-ads-live/active` (default `baseline`).
