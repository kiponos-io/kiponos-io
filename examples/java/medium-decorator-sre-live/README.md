# medium-decorator-sre-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-sre-live/active` (default `baseline`).
