# medium-state-travel-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-travel-live/active` (default `baseline`).
