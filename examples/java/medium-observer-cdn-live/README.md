# medium-observer-cdn-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Cdn War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-cdn-live/active` (default `baseline`).
