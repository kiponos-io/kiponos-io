# medium-decorator-banking-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Banking War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-banking-live/active` (default `baseline`).
