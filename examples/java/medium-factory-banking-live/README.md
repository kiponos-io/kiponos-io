# medium-factory-banking-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Banking War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-banking-live/active` (default `baseline`).
