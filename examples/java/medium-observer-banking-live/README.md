# medium-observer-banking-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Banking War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-banking-live/active` (default `baseline`).
