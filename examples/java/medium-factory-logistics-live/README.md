# medium-factory-logistics-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-logistics-live/active` (default `baseline`).
