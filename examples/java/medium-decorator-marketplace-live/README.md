# medium-decorator-marketplace-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Marketplace War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-marketplace-live/active` (default `baseline`).
