# medium-decorator-travel-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-travel-live/active` (default `baseline`).
