# medium-strategy-insurance-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Insurance War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-insurance-live/active` (default `baseline`).
