# medium-state-cdn-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Cdn War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-cdn-live/active` (default `baseline`).
