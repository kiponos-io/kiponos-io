# medium-observer-edtech-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Edtech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-edtech-live/active` (default `baseline`).
