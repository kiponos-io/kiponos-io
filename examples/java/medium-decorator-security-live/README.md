# medium-decorator-security-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Security War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-security-live/active` (default `baseline`).
