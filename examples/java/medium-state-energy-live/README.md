# medium-state-energy-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Energy War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-energy-live/active` (default `baseline`).
