# aha-http-timeout

Read HTTP client timeout live

```bash
cd examples/java/aha-http-timeout
cp kiponos.local.env.example kiponos.local.env
./gradlew test run
```

Hub key: `examples/aha-http-timeout/timeout-ms` (default `3000`).
