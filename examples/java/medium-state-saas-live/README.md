# medium-state-saas-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Saas War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-saas-live/active` (default `baseline`).
