# medium-chain-media-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-media-live/active` (default `baseline`).
