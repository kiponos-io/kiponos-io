# medium-decorator-ads-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Ads War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-ads-live/active` (default `baseline`).
