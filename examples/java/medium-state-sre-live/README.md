# medium-state-sre-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-sre-live/active` (default `baseline`).
