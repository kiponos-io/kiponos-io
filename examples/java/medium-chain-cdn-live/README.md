# medium-chain-cdn-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Cdn War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-cdn-live/active` (default `baseline`).
