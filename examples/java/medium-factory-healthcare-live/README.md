# medium-factory-healthcare-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Healthcare War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-healthcare-live/active` (default `baseline`).
