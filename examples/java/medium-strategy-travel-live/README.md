# medium-strategy-travel-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-travel-live/active` (default `baseline`).
