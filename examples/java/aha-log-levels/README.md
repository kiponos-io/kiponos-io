# aha-log-levels

Root log level live

```bash
cd examples/java/aha-log-levels
cp kiponos.local.env.example kiponos.local.env
./gradlew test run
```

Hub key: `examples/aha-log-levels/level` (default `INFO`).
