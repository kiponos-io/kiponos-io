# medium-state-fintech-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Fintech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-fintech-live/active` (default `baseline`).
