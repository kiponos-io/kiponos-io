# medium-state-retail-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-retail-live/active` (default `baseline`).
