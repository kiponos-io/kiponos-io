# aha-cache-ttl

Session cache TTL live

```bash
cd examples/java/aha-cache-ttl
cp kiponos.local.env.example kiponos.local.env
./gradlew test run
```

Hub key: `examples/aha-cache-ttl/ttl-seconds` (default `300`).
