# medium-observer-media-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-media-live/active` (default `baseline`).
