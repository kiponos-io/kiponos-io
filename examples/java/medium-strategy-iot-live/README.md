# medium-strategy-iot-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Iot War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-iot-live/active` (default `baseline`).
