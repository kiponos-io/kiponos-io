# medium-factory-fintech-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Fintech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-fintech-live/active` (default `baseline`).
