# medium-chain-insurance-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Insurance War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-insurance-live/active` (default `baseline`).
