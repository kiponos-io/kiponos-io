# medium-decorator-healthcare-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Healthcare War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-healthcare-live/active` (default `baseline`).
