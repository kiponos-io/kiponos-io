# medium-proxy-fintech-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Fintech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-fintech-live/active` (default `baseline`).
