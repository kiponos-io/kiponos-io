# medium-chain-iot-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Iot War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-iot-live/active` (default `baseline`).
