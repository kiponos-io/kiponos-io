# medium-state-ads-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Ads War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-ads-live/active` (default `baseline`).
