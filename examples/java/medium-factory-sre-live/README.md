# medium-factory-sre-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-sre-live/active` (default `baseline`).
