# medium-observer-security-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Security War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-security-live/active` (default `baseline`).
