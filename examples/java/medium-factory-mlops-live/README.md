# medium-factory-mlops-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Mlops War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-mlops-live/active` (default `baseline`).
