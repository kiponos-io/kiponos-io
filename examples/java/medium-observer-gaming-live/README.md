# medium-observer-gaming-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Gaming War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-gaming-live/active` (default `baseline`).
