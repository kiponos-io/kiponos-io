# Super Pattern: Live Proxy (Access Gate)

Enable/disable and role allow-list for a sensitive path — live from the hub.

```bash
cd examples/java/pattern-proxy-live-access && ./gradlew test run --args=operator
```
