# medium-strategy-banking-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Banking War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-banking-live/active` (default `baseline`).
