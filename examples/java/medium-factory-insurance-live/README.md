# medium-factory-insurance-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Insurance War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-insurance-live/active` (default `baseline`).
