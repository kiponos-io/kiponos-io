# medium-strategy-media-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-media-live/active` (default `baseline`).
