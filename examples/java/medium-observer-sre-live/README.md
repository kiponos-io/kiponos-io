# medium-observer-sre-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-sre-live/active` (default `baseline`).
