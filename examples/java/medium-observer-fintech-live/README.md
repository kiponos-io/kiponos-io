# medium-observer-fintech-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Fintech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-fintech-live/active` (default `baseline`).
