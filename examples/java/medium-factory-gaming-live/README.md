# medium-factory-gaming-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Gaming War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-gaming-live/active` (default `baseline`).
