# angular-sdk-status-wall

**Angular Status Wall Fed by the Same Hub as Everyone Else**

Multi-SDK Super Pattern peer set:
- **Java** (this directory) — `./gradlew run` / `./gradlew test`
- **Python** — `examples/python/angular-sdk-status-wall/`
- **Node (angular)** — `examples/node/angular-sdk-status-wall/`

## Hub leaf

`ops/status-headline` (default `steady`)

## Run (Java)

```bash
export KIPONOS_ID=… KIPONOS_ACCESS=…
export KIPONOS="['MyApp']['1.0']['Dev']['base']"
./gradlew test
./gradlew run
```

## Story

`docs/examples/medium-drafts/angular-sdk-status-wall.md`
