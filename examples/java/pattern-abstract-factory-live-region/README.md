# Super Pattern: Live Abstract Factory (Region)

Choose US vs EU product family (currency + tax) from the hub.

```bash
cd examples/java/pattern-abstract-factory-live-region && ./gradlew test run
```
