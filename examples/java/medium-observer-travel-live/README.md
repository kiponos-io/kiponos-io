# medium-observer-travel-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Travel War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-travel-live/active` (default `baseline`).
