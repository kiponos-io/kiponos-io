# medium-observer-ads-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Ads War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-ads-live/active` (default `baseline`).
