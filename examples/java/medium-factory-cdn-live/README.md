# medium-factory-cdn-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Cdn War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-cdn-live/active` (default `baseline`).
