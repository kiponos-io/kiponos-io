# medium-proxy-media-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-media-live/active` (default `baseline`).
