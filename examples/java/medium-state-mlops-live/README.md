# medium-state-mlops-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Mlops War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-mlops-live/active` (default `baseline`).
