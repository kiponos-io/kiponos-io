# medium-strategy-healthcare-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Healthcare War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-healthcare-live/active` (default `baseline`).
