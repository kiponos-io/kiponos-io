# medium-chain-energy-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Energy War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-energy-live/active` (default `baseline`).
