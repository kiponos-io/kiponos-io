# medium-decorator-media-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Media War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-media-live/active` (default `baseline`).
