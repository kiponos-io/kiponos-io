# medium-proxy-saas-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Saas War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-saas-live/active` (default `baseline`).
