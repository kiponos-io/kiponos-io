# medium-proxy-retail-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Retail War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-retail-live/active` (default `baseline`).
