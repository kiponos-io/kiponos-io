# medium-state-healthcare-live

Live Kiponos posture example for Medium: **The State Pattern Transition Policy Live — Healthcare War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-state-healthcare-live/active` (default `baseline`).
