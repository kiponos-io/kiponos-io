# medium-observer-saas-live

Live Kiponos posture example for Medium: **The Observer Pattern Subscribe Topology Live — Saas War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-observer-saas-live/active` (default `baseline`).
