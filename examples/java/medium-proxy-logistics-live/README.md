# medium-proxy-logistics-live

Live Kiponos posture example for Medium: **The Proxy Pattern Gate Access Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-proxy-logistics-live/active` (default `baseline`).
