# medium-strategy-edtech-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Edtech War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-edtech-live/active` (default `baseline`).
