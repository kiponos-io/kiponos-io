# medium-chain-ads-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Ads War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-ads-live/active` (default `baseline`).
