# medium-chain-sre-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Sre War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-sre-live/active` (default `baseline`).
