# medium-strategy-security-live

Live Kiponos posture example for Medium: **The Strategy Pattern Swap Algorithms Live — Security War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-strategy-security-live/active` (default `baseline`).
