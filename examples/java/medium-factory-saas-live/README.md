# medium-factory-saas-live

Live Kiponos posture example for Medium: **The Factory Pattern Select Product Channel Live — Saas War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-factory-saas-live/active` (default `baseline`).
