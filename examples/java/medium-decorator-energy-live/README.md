# medium-decorator-energy-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Energy War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-energy-live/active` (default `baseline`).
