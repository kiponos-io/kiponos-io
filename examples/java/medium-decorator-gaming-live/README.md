# medium-decorator-gaming-live

Live Kiponos posture example for Medium: **The Decorator Pattern Rebuild Wrapper Chains Live — Gaming War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-decorator-gaming-live/active` (default `baseline`).
