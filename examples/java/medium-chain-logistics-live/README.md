# medium-chain-logistics-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Logistics War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-logistics-live/active` (default `baseline`).
