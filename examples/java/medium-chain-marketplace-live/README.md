# medium-chain-marketplace-live

Live Kiponos posture example for Medium: **The Chain of Responsibility Pattern Reorder Handlers Live — Marketplace War Room**

```bash
./gradlew test run
```

Hub path: `examples/medium-chain-marketplace-live/active` (default `baseline`).
